This is GITHUB Demo.

git status
git add .
git commit -m "your message"
git push

git pull

git clone url

check url 
git remote -v

add branch
git checkout -b branchname

check on which branch you are on
git branch

git status
git add .
git commit -m "your message"
git push origin branchname/main

change branch
git checkout branchname/main

git merge branchname/main
git status
git add .
git commit -m "your message"
git push origin branchname/main
(when merging branches we have to be in the branch where we want to merge it)

git branch
git pull origin branchname/main